version: "3.9"

services:
  api:
    build: ./api
    ports:
      - "8080:8080"
    environment:
      - PORT=8080

  app:
    build: ./app
    ports:
      - "5173:5173"
    depends_on:
      - api
